<?php

return array(
    /* Database connection name to use for queue jobs. */
    'connection' => null,

    /* Max number of cycles a worker can be idle before being killed. Set to 0 to disable. */
    'max_idle_cycle' => 0,

    /* Max number of jobs a worker can process before being killed. Set to 0 to disable. */
    'max_jobs' => 1000,

    /* Minimum number of workers to maintain. */
    //'min_workers' => 4,

    /* Maximum number of workers to maintain. */
    //'max_workers' => 20,
);
